package com.mvicente.dware.drivers;

import com.mvicente.dware.utils.WordKey;

public class DwareDriver
{
    public static boolean buildWordKey(WordKey wKey, String wordFilePath)
    {
        boolean success = false;

        return success;
    }
}
